[project]
name = "fbac_scripts"
version = "1.1.0"
description = "A set of scripts for managing FBAC Avatars."
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
    "watchdog>=6.0.0",
]

[dependency-groups]
dev = [
    "types-pillow>=10.2.0.20240822",
    "unittest-xml-reporting>=4.0.0",
]
