[project]
name = "build-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pillow>=11.3.0",
]

[dependency-groups]
dev = [
    "types-pillow>=10.2.0.20240822",
]
